# v1.8.1 验证记录

验证日期：2026-09-17。

自动回归结果：**16 passed**，测试进程正常退出，exit code 0。

| 检查 | 结果 |
| --- | --- |
| Legacy 变换与独立旋转矩阵计算在多个 Cmd 姿态下比较 | 通过 |
| 编辑副本与原文件隔离，RPY 与矩阵同步 | 通过 |
| 文件错槽位、非法轴、非有限零位、无效旋转 | 通过 |
| 历史来源文件名、路径、指纹不同或缺失均不阻止输出；缺文件仍停用 | 通过 |
| GUI 原值/副本切换，Base 与三维视图共用参数 | 通过 |
| 副本设置恢复、原文件改变后旧副本不恢复 | 通过 |
| RGB 自动开关先于手动值，范围与步进校验，写入失败后可继续 | 通过 |
| 参数线程执行慢写入时，最新帧生产不被该线程阻塞 | 通过 |
| RGB 参数按设备序列号恢复，自动模式限制相应手动输入 | 通过 |
| 90/50 控制帧及 GBK 文本逐字节/任意二段分包解析 | 通过 |
| 合成 AprilTag ID 3 检测、PnP 距离、100 mm 独立偏移 | 通过 |
| MainWindow 启动、编码器不参与 Cmd 换算、Home 与标定独立 | 通过 |
| 模拟相机持续采集、慢识别、参数写入、关闭窗口释放线程 | 通过 |
| 较早参数写入的成功回执不会因新操作而丢失持久化 | 通过 |

新增两项：本次用户上传文件首次启动可直接输出 Base，编辑副本与三维视图同步；历史设置中的任意相机示例路径得到保留。均通过。

全部 Python 文件通过编译检查；PySide6、OpenCV、RealSense 模块可导入。

Qt offscreen 下检查了主界面、标定编辑区和全部相机参数的模拟界面；使用中文字体核对文字与布局。相机参数区按需展开；Base 结果置于右侧上部。窗口整体保留双向滚动。

pantilt.py 与 v1.7 保持逐字节一致；v1.7 单帧发送、限频、最新目标覆盖和 GBK 解析实现未重写。包中的两份默认标定 JSON 与本次用户上传文件逐字节一致。旋转正交性、行列式和不同旋转表示间一致性检查通过，误差在浮点数舍入量级。

验证平台为 Linux Python 环境。未连接 D435 或物理串口设备；Qt offscreen 与模拟传感器测试不代表已完成 Windows 实物验证。用户确认本次标定文件已验证；此次修订未重新进行现场曝光画质、真实帧率、运动效果或定位精度验证。
